#include "errors.h"

namespace errors {
	EofError eofError;
	OpenError openError;
	ForkError forkError;
	WaitError waitError;
	ExecError execError;
	UnknownError unknownError;
	ArgumentError argumentError;
}